# 【FLOW-001】 1_今月の給与対象者取得（21日、27日起動）

## 1. 基本情報 (Overview)

| 項目 | 内容 |
| :--- | :--- |
| **フロー名** | 1_今月の給与対象者取得（21日、27日起動） |
| **管理番号 / ID** | FLOW-001 |
| **Yoom管理URL** | `https://yoom.fun/24ebae21-5ba8-4c38-9f3c-f5f8574facee/workflow_templates/312795/edit` |
| **ステータス** | 🟢 本番運用中 |
| **所有者 / 管理者** | 王 旭陽 |
| **最終更新日** | 2026-09-15 |

---

## 2. 目的と概要 (Purpose & Description)

* **ビジネス目的:**  
  今月の給与対象者の情報を自動で取得し、Slackへ請求詳細を送るため
* **処理の概要:**  
  毎月指定されたタイミング（21日・27日）に自動起動し、対象メンバーの契約・給与・SmartHR情報をもとにタイムカードや進捗管理シートを生成・更新し、Slackでの確認依頼や情報共有を自動化する。

---

## 3. 動作条件・連携アカウント (Triggers & Integrations)

### 3.1 連携アカウント (Connected Accounts)
* **Google スプレッドシート:** `ou@ls-mirai.com`
* **Slack:** `ou@ls-mirai.com`
* **SmartHR:** `ou@ls-mirai.com`

### 3.2 起動トリガー (Trigger Events)
* **アプリ:** なし　※スケジュールトリガー
* **イベント:** 指定の日付になったら
* **スケジュール / 実行間隔:** 毎月21日、27日8時に実行

---

## 4. フロー詳細構成 (Flow Steps & Data Flow)

```mermaid
graph TD
    %% 起動トリガー
    Trigger[スケジュールトリガー<br>毎月21日・27日 8:00] --> S1[稼働月を取得]
    S1 --> S2[給与支払日取得]
    S2 --> S42[42_現場メンバー契約・給与マスタースプレッドシートの情報を取得]

    %% 分岐1: 進行進捗シート存在確認
    S42 --> Branch43{43_現場請求・給与進捗シートの<br>プロパティが空でないか？}
    Branch43 -- 取得した値が空ではない --> S43_Move[移動: 45]
    Branch43 -- 空である --> S6[6_現場請求・給与進捗シートをテンプレートからコピー]

    S6 --> S41[41_現場請求・給与進捗シートの名前を正しくする]
    S41 --> S45
    S43_Move --> S45[45_レコード番号シートに請求番号セルの数式を更新]

    S45 --> S44[44_実行タイミングによって月末or20日の文字列を生成]
    S44 --> S_DB1[DB操作: 給与支給対象全員の中に<br>請求対象要確認のものを取得]

    %% 分岐2: 請求対象要確認のハンドリング
    S_DB1 --> Branch_Req{請求対象要確認があるか？<br>取得した値が空ではない}
    
    %% 要確認がある場合（Slack通知＆ループ）
    Branch_Req -- YES --> S_Slack_Req[1_チャンネルに請求情報確認依頼メッセージを送る]
    S_Slack_Req --> Loop1_Start((ループ: 複数の請求対象))
    Loop1_Start --> S_Slack_Mention1[Slackのユーザーグループメンションコマンド作成]
    S_Slack_Mention1 --> S_Slack_Thread1[スレッドにメッセージを送る]
    S_Slack_Thread1 --> Loop1_End((ループ終了))
    Loop1_End --> S_Move46[移動: 46]

    %% 要確認なし・または完了後
    Branch_Req -- NO --> S_DB46[46_要確認以外の給与支払対象を取得]
    S_Move46 --> S_DB46

    %% メイン処理ループ
    S_DB46 --> Loop2_Start((ループ: 要確認以外の給与支払対象))

    %% ループ内 A: フル待機判定分岐
    Loop2_Start --> Branch_Wait{フル待機かどうか？<br>値と等しい ◯}
    Branch_Wait -- YES --> S_DB11[11_すでにポストされた請求対象外Slackメッセージを取得]
    S_DB11 --> Branch12{12_請求対象外Slackメッセージがあるか？}
    Branch12 -- YES --> S_Move15[移動: 15]
    Branch12 -- NO --> S_Slack13[13_チャンネルに請求対象外のメッセージを送る]
    S_Slack13 --> S_TS14[14_SlackタイムスタンプをURLへ置換]
    S_TS14 --> S_DB15[15_請求対象外の情報を進捗管理シートへ書き出す]
    S_Move15 --> S_DB15
    S_DB15 --> S_Slack16[16_請求対象外のスレッドに詳細メッセージを送る]
    S_Slack16 --> Loop2_Next((次の対象へ))

    %% ループ内 B: 通常メンバー処理
    Branch_Wait -- NO --> S_DB2[2_契約情報を取得]
    S_DB2 --> S_DB3[3_給与情報を取得]
    S_DB3 --> S_DB4[4_固定交通費情報を取得]
    S_DB4 --> S_SHR10[10_smartHRの従業員情報を検索]
    S_SHR10 --> S_TC24[24_タイムカード作成・移動]
    S_TC24 --> S_TC25[25_タイムカード情報取得]
    S_TC25 --> S_TC26[26_シート名更新]
    S_TC26 --> S_TC_SetInfo[タイムカードに値入力<br>社員番号/名前/給与種類/締め日]
    
    S_TC_SetInfo --> S_DB5[5_同じ請求先で先に記載されたレコードがあるか検索]
    S_DB5 --> S_ID7[7_新しい請求IDを取得]

    %% ループ内 C: 締め日による切り替え (Switch)
    S_ID7 --> Switch8{8_締め日によって切り替え}
    Switch8 -- 20日 --> S_D20[稼働月スタート/エンド日取得_20日]
    S_D20 --> S_TC32[32_タイムカード入力: 今月スタート日_20日]
    S_TC32 --> S_Move39[移動: 35]

    Switch8 -- 月末 --> S_DEnd[稼働月エンド日取得_月末]
    S_DEnd --> S_D33[33_稼働月スタート日取得_月末]
    S_D33 --> S_TC34[34_タイムカード入力: 今月スタート日_月末]
    
    S_Move39 --> S_TC35[35_タイムカード入力: 下限時間〜精算単位]
    S_TC34 --> S_TC35

    S_TC35 --> S_DB47[47_進捗シートのレコードを取得]
    S_DB47 --> Branch48{48_すでに進捗シートに<br>レコードが存在するか？}
    Branch48 -- YES --> S_DB48_Up[48_レコードの契約と給与情報を更新]
    Branch48 -- NO --> S_DB9[9_情報を進捗管理のシートへ書き出す]

    S_DB48_Up --> Branch17
    S_DB9 --> Branch17{17_同じ請求先のレコードが存在するか？}

    %% ループ内 D: Slack請求スレッド作成・追加
    Branch17 -- YES --> S_DB18[18_SlackメッセージURL・TSをコピー追加]
    S_DB18 --> S_Move22[移動: 22]

    Branch17 -- NO --> S_Slack19[19_チャンネルに請求先ごとの親メッセージを送る]
    S_Slack19 --> S_TS20[20_Slackタイムスタンプ_URL置換]
    S_TS20 --> S_DB21[21_Slack情報シート追加]
    
    S_Move22 --> S_DB22[22_詳細スレッドの投稿先Slackメッセージ取得]
    S_DB21 --> S_DB22
    S_DB22 --> S_Slack23[23_Slackスレッドへ請求詳細を送る]
    S_Slack23 --> Loop2_Next
```

### ステップ詳細

#### 🔹 Trigger: GitHub Issue作成
* **設定パラメータ:**
  * Repository: `organization/main-repo`
  * State: `Open`
* **取得する主な変数 (Variables):**
  * `{{issue.title}}`: Issueのタイトル
  * `{{issue.body}}`: Issueの本文
  * `{{issue.user.login}}`: 作成者名
  * `{{issue.html_url}}`: IssueのURL

---

#### 🔹 Step 1: AI要約・緊急度判定 (Yoom AI)
* **アクション:** テキスト生成 / 要約
* **プロンプト / 設定入力:**
  > 以下のIssue内容を要約し、緊急度を「High」「Normal」「Low」のいずれかで判定してください。
  > タイトル: `{{issue.title}}`
  > 本文: `{{issue.body}}`
* **出力変数 (Outputs):**
  * `{{step1.summary}}`: 要約テキスト
  * `{{step1.priority}}`: 判定された優先度

---

#### 🔹 Step 2: 条件分岐 (Branching)
* **分岐条件:**
  * 条件A: `{{step1.priority}}` 等しい `High` ➔ **Step 3-A** へ
  * 条件B: 上記以外 ➔ **Step 3-B** へ

---

#### 🔹 Step 3-A: Slack通知 (緊急)
* **対象:** Slack `#urgent-alert`
* **送信メッセージ:**
  ```text
  🚨 【緊急Issue通知】
  タイトル: {{issue.title}}
  作成者: {{issue.user.login}}
  要約: {{step1.summary}}
  URL: {{issue.html_url}}
  ```

---

## 5. エラーハンドリング・例外処理 (Exception Handling)

* **エラー時の通知先:** 運用チームメール (`ops-alert@example.com`) / Slack `#yoom-system-log`
* **リカバリ手順:**  
  1. API制限等で失敗した場合は、Yoom管理画面の「実行履歴」から該当ログを確認。
  2. 原因解決後、実行履歴詳細の「再実行」ボタンを押下する。

---

## 6. バージョン変更履歴 (Changelog)

| バージョン | 変更年月日 | 変更者 | 変更内容・理由 | PR / Issueリンク |
| :--- | :--- | :--- | :--- | :--- |
| **v1.1.0** | 2026-09-15 | 佐藤 花子 | AIによる自動要約ステップ (Step 1) を追加 | `#PR-102` |
| **v1.0.1** | 2026-08-20 | 山田 太郎 | Slackの通知先チャンネルを `#general` から `#dev` へ変更 | `#PR-45` |
| **v1.0.0** | 2026-08-01 | 山田 太郎 | 新規作成・本番運用開始 | `#PR-12` |
